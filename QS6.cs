/*using System;
public class SUM
{
	public static void Main()
	{
		int[] arr = new int[5];
         for(int i=0; i<arr.Length; i++)
        {
			Console.WriteLine("enter " + i + " number: ", i);
			arr[i] = Convert.ToInt32(Console.ReadLine());
        }
		int num = arr[0];
		for (int i = 0; i < arr.Length; i++)
        {
            if (num > arr[i])

            {
                num = arr[i];
            }
        }
        Console.WriteLine("the smallest integer in list is: " + num);



	}
}*/